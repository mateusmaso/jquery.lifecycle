jquery.lifecycle
================

```javascript
$(element).lifecycle({
  insert: function() { ... }, 
  remove: function() { ... },
  change: function(attribute) { ... }
});
$(element).unlifecycle();
```
